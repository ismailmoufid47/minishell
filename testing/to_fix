
- don't include hidden files in * by itself, eg.: ls *

- exit should throw error when the first arg is bigger than long

- sort the * output

- heredoc signals

- heredoc with no command segfaults

- change last status code when command not found or other system calls failure

- don't expand when the delimiter has quotes 
